# =========================================
# 🎮 PokéArena API - Render Deploy Config
# =========================================
#
# Este arquivo configura automaticamente todos os recursos necessários na Render.
# A Render detecta este arquivo na raiz do repositório e cria:
#   - Web Service (Backend NestJS)
#   - PostgreSQL Database
#   - Variáveis de ambiente conectadas automaticamente
#
# Como usar:
#   1. Faça commit deste arquivo: git add render.yaml && git commit -m "Add Render config"
#   2. Push para o GitHub: git push origin main
#   3. Na Render: Dashboard → New → Blueprint → Conectar repositório
#   4. Render detecta o render.yaml automaticamente e cria tudo com um clique!
#
# Documentação: https://render.com/docs/infrastructure-as-code
# =========================================

services:
  # =========================================
  # 🚀 Web Service (Backend API)
  # =========================================
  - type: web
    name: poke-arena-back
    runtime: node
    region: oregon  # us-west (mais barato) - alternativas: ohio (us-east), frankfurt (eu-central)
    plan: free  # Planos disponíveis: free, starter, standard, pro

    # Branch principal para auto-deploy
    branch: main

    # Build Command: instala dependências e compila TypeScript
    # --production=false: instala devDependências (necessário para @nestjs/cli)
    buildCommand: yarn install --production=false && yarn build

    # Start Command: roda migrations ANTES de iniciar a aplicação
    # Garante que o banco está sempre atualizado antes do app iniciar
    # Usa caminho absoluto porque o working dir pode ser diferente
    startCommand: yarn migration:run && yarn start

    # Health Check: endpoint que a Render monitora continuamente
    # Se falhar, reinicia automaticamente o serviço
    healthCheckPath: /health

    # Auto-deploy: deploy automático quando há push na branch principal
    autoDeploy: true

    # =========================================
    # 🔧 Variáveis de Ambiente
    # =========================================
    envVars:
      # ---------------------------------------
      # Configurações da Aplicação
      # ---------------------------------------

      - key: NODE_ENV
        value: production

      - key: NODE_VERSION
        value: 24.8.0

      - key: PORT
        value: 3000

      - key: API_PREFIX
        value: ""  # Vazio por padrão - pode configurar como "/api" se necessário

      # ---------------------------------------
      # Configurações do Banco de Dados
      # ---------------------------------------
      # A Render injeta automaticamente as credenciais do PostgreSQL criado abaixo
      # usando a sintaxe "fromDatabase". Isso significa:
      #   ✅ Zero hardcode de senhas
      #   ✅ Conexão segura automática
      #   ✅ Credenciais gerenciadas pela Render

      - key: DATABASE_HOST
        fromDatabase:
          name: pokearena-db
          property: host

      - key: DATABASE_PORT
        fromDatabase:
          name: pokearena-db
          property: port

      - key: DATABASE_USER
        fromDatabase:
          name: pokearena-db
          property: user

      - key: DATABASE_PASSWORD
        fromDatabase:
          name: pokearena-db
          property: password

      - key: DATABASE_NAME
        fromDatabase:
          name: pokearena-db
          property: database

      # IMPORTANTE: DATABASE_SYNC deve ser FALSE em produção!
      # Use migrations (TypeORM) para gerenciar schema do banco
      - key: DATABASE_SYNC
        value: false

      # DATABASE_LOGGING: false em produção para melhor performance
      # Ative (true) apenas para debugging
      - key: DATABASE_LOGGING
        value: false

      # ---------------------------------------
      # Configurações de Logging
      # ---------------------------------------

      - key: LOG_LEVEL
        value: info  # Níveis: debug, info, warn, error

      - key: LOG_FORMAT
        value: json  # Formato estruturado para análise de logs

      # ---------------------------------------
      # Configurações do Swagger
      # ---------------------------------------

      - key: SWAGGER_TITLE
        value: PokéArena API

      - key: SWAGGER_DESCRIPTION
        value: API para gerenciamento de pokémons e batalhas

      - key: SWAGGER_VERSION
        value: 1.0.0

# =========================================
# 🗄️ PostgreSQL Database
# =========================================
databases:
  - name: pokearena-db
    databaseName: pokearena_db
    plan: free  # Planos: free (256MB), starter (1GB), standard (10GB), pro (50GB)
    region: oregon  # Mesma região do web service para menor latência

    # A Render cria automaticamente:
    #   - Database com nome "pokearena_db"
    #   - Usuário com permissões completas
    #   - Senha segura gerada automaticamente
    #   - Todas as credenciais são injetadas no web service via "fromDatabase"